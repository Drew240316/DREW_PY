print("키워드 인수는 key=value 형식으로 순서와 무관하게 함수에 값을 전달할 수 있으며, \n 딕셔너리를 넘길 때는 ** 언패킹이 필요합니다.")

def personal_info_2(name2, age2, address2):
    """ 키워드(이름)인수는 호출 부분이 포인트다.
        순서가 변경되도 실제 값을 넘길 수 있다.
        - 이터러블 객체는 모두 사용 가능하다.
        - 딕셔너리 객체를 사용하려면, 언패킹을 사용한다."""
    print('이름: ', name2)
    print('나이: ', age2)
    print('주소: ', address2)



personal_info_2(age2 = 33, name2 = 'mimi' , address2 = '서울특별시')

print("------------------(위) 키워드 인수로 함수 호출 하기-----------------------------")




def personal_info(name, age, address):
    """ 키워드 인수 언패킹 예제
        - **는 딕셔너리를 언패킹할 때 사용 (key=value 형태로 해석)
        - 언패킹 방법은 두 가지가 있다:
            1. 변수에 딕셔너리를 저장하고 **로 언패킹
            2. 함수 호출 시 딕셔너리를 직접 **로 언패킹
    """
    print('이름:', name)
    print('나이:', age)
    print('주소:', address)

# 딕셔너리 언패킹 방법 1: 딕셔너리를 변수에 저장하고 언패킹
# dic = {'name': '홍길동', 'age': 30, 'address': '서울시 용산구 이촌동'}
# personal_info(**dic)

# 딕셔너리 언패킹 방법 2: 함수 호출 시 직접 **로 언패킹
personal_info(**{'name': '홍길동', 'age': 30, 'address': '서울시 용산구 이촌동'})

# 함수 설명 출력
print("\n 함수 설명:")
print(personal_info.__doc__)

print("------------------(위) 딕셔너리로 언패킹-----------------------------")



##**는 **"키워드 인수들을 딕셔너리로 받아라"**는 문법
## 변수 이름 다양하게 구성 가능
###...args, info, data, kwargs → 전부 네가 정하는 변수 이름